# Дизайн-бандл — внутренний материал студии: разбор созвона с цитатами
# клиента, открытые вопросы, фото основателя. В публичный репозиторий не идёт.
design_handoff_sravniplitku_landing/

# Исходники скриншотов сервиса: в репозиторий идут уменьшенные копии
# в assets/img/steps/, оригиналы лежат локально
screenshots/

# Пакеты ТЗ и дизайн-аудита — входной материал, а не продукт.
# Лежат локально рядом с проектом.
design_handoff_landing_fixes/

# ТЗ на блок об основателе: эталон, исходное фото и описание.
# В репозиторий идёт готовый блок и assets/img/founder.jpg
founder_block/

# ТЗ на блок «Салонам / Поставщикам»: эталон и описание
segment_block/

# ТЗ на энергию первого экрана и на четыре блока
energy_pilot/
fix02/
compare_block/
video_block/
motion/
steps_block/
hero_fit/

# ТЗ на первый экран с полным видео и на плеер длинного видео:
# эталоны в HTML, кадр интерфейса, рантайм для их просмотра
design_handoff_hero_video_player/

# ТЗ на выравнивания и типографику первого экрана — входной материал,
# как и остальные пакеты ТЗ выше
HERO_FIXES.md

# ТЗ на блок «Пять действий»: эталон рельса и описание
steps_rail/

# Заявки с персональными данными и метки антифлуда
api/logs/*.log
api/logs/throttle-*.txt

# Исходные видео от клиента (40 МБ в 1080p): в репозиторий идут
# пережатые копии в assets/video/. Слеш в начале обязателен:
# без него шаблон зацепил бы и assets/video/.
/video/

# Служебное
.DS_Store
Thumbs.db
desktop.ini
*.log
node_modules/
.vscode/
.idea/

# Исходники рендеров от клиента (74 МБ): в репозиторий идут пережатые
# копии в assets/img/compare, assets/img/sources и assets/img/works
visual/

# Правки текста от клиента — входной материал, как и пакеты ТЗ выше
*.docx
